#include "Config.hpp"

/* parsing the file and retrieve all informations needed
   in class Config
*/

void Config::create(void)
{
    return;
}

/* parsing the file until the end or until finding settingName to
      return his value
    usage: config.find("fps", "window.config") (-> "60")
*/
    
std::string Config::find(std::string keyword, std::string fileName)
{
    std::string value;

    return value;
}

void Config::replace(std::string newKeyword, std::string keyword, std::string fileName)
{
    return;
}
